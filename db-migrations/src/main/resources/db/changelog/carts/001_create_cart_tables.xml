<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <changeSet id="001_create_cart_tables" author="admin">
        <createTable tableName="carts">
            <column name="id" type="UUID" defaultValueComputed="gen_random_uuid()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_id" type="UUID"/>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createTable tableName="cart_items">
            <column name="id" type="SERIAL">
                <constraints primaryKey="true"/>
            </column>
            <column name="cart_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="product_id" type="INTEGER">
                <constraints nullable="false"/>
            </column>
            <column name="quantity" type="INTEGER">
                <constraints nullable="false"/>
            </column>
            <column name="price_at_time" type="NUMERIC(10,2)"/>
        </createTable>

        <sql>
            ALTER TABLE cart_items
                ADD CONSTRAINT fk_cart_items_cart_id
                    FOREIGN KEY (cart_id) REFERENCES carts(id);
        </sql>

        <sql>
            ALTER TABLE cart_items
                ADD CONSTRAINT chk_cart_item_quantity
                    CHECK (quantity > 0);
        </sql>

        <createIndex tableName="cart_items" indexName="idx_cart_items_product_id">
            <column name="product_id"/>
        </createIndex>
    </changeSet>

    <changeSet id="add-unique-cart-items" author="fix">
        <sql>
            ALTER TABLE cart_items
                ADD CONSTRAINT uk_cart_items_cart_id_product_id
                    UNIQUE (cart_id, product_id);
        </sql>
    </changeSet>

    <changeSet id="002_add_unique_user_id" author="fix">
        <sql>
            ALTER TABLE carts
                ADD CONSTRAINT uk_carts_user_id
                    UNIQUE (user_id);
        </sql>
    </changeSet>
</databaseChangeLog>